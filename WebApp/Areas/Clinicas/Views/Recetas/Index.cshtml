@model IEnumerable<WebApp.Areas.Clinicas.Models.recetas>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Clinicas/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.observaciones)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.consultas.descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.observaciones)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.consultas.descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idRecetas }) |
            @Html.ActionLink("Details", "Details", new { id=item.idRecetas }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idRecetas })
        </td>
    </tr>
}

</table>
